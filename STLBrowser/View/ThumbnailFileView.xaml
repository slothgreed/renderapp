<UserControl x:Class="STLBrowser.View.ThumbnailFileView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:control="clr-namespace:KI.Foundation.Control;assembly=KI.Foundation"
       >
    <StackPanel>
        <Image Source="{Binding Model.Thumbnail}" Width="128" Height="128" Margin="2"/>

        <TextBlock Text="{Binding Model.FileName}" Height="25" Margin="2" TextAlignment="Center" AllowDrop="True">
                    <i:Interaction.Behaviors>
                <control:DragBehavior DragData="{Binding Model.FilePath}"/>
            </i:Interaction.Behaviors>
        </TextBlock>
    </StackPanel>
</UserControl>
