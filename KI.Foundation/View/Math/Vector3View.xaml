<UserControl x:Class="KI.Foundation.View.Vector3View"
             xmlns:resc="clr-namespace:KI.Foundation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="/KI.Foundation;component/Resource.xaml"/>
    </UserControl.Resources>
    <StackPanel >
        
        <WrapPanel>
            <TextBlock Text="{Binding Key}" Style="{StaticResource NameTemplate}"/>
            <TextBlock Text="X" Style="{StaticResource VectorXStyle}"/>
            <TextBox Text="{Binding Value.X, StringFormat=N3}"/>
            <TextBlock Text="Y" Style="{StaticResource VectorYStyle}"/>
            <TextBox Text="{Binding Value.Y, StringFormat=N3}" />
            <TextBlock Text="Z"  Style="{StaticResource VectorZStyle}"/>
            <TextBox Text="{Binding Value.Z, StringFormat=N3}" />
        </WrapPanel>
    </StackPanel>
</UserControl>
