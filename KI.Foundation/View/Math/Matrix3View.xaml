<UserControl x:Class="KI.Foundation.View.Matrix3View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="Resource.xaml"/>
    </UserControl.Resources>
    <StackPanel>
        <TextBlock Text="{Binding Key}" Style="{StaticResource NameTemplate}"/>
        <WrapPanel>
            <TextBox Text="{Binding Value.M11, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M12, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M13, StringFormat=N3}"/>
        </WrapPanel>
        <WrapPanel>
            <TextBox Text="{Binding Value.M21, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M22, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M23, StringFormat=N3}"/>
        </WrapPanel>
        <WrapPanel>
            <TextBox Text="{Binding Value.M31, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M32, StringFormat=N3}"/>
            <TextBox Text="{Binding Value.M33, StringFormat=N3}"/>
        </WrapPanel>
    </StackPanel>
</UserControl>
