<ResourceDictionary x:Class="KI.Foundation.Themes.ThumbnailFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:control="clr-namespace:KI.Foundation.Control">
    <Style TargetType="{x:Type control:ThumbnailFile}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type control:ThumbnailFile}">
                    <StackPanel Margin="3">
                        <Button Content="ここには画像が入る" Width="128" Height="128"/>
                        <Image Source="{Binding ImagePath}" Width="128" Height="128"/>
                        <TextBlock Text="{Binding FilePath}" x:Name="PART_FilePath" TextAlignment="Center"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
