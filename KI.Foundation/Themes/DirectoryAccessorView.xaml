<ResourceDictionary x:Class="KI.Foundation.Themes.DirectoryAccessorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:control="clr-namespace:KI.Foundation.Control"
              >
    <Style TargetType="{x:Type control:DirectoryAccsessor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type control:DirectoryAccsessor}">
                    <DockPanel>
                        <TextBlock Text="{Binding FolderPath}" ToolTip="{TemplateBinding FolderPath}"/>
                        <Button Content="参照" x:Name="PART_RefButton" DockPanel.Dock="Right" Width="48"/>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
