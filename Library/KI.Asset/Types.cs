namespace KI.Asset
{
    /// <summary>
    /// 頂点格納種類
    /// </summary>
    public enum IndexStoreType
    {
        Normal,     // DrawArrays の順番で入っている
        VertexArray // VertexArray状態で入っている
    }
}
