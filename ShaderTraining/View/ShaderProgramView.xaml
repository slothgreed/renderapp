<UserControl x:Class="ShaderTraining.View.ShaderProgramView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShaderTraining.View"
             xmlns:presenter="clr-namespace:KI.Presenter.Controls;assembly=KI.Presenter"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBox Text="{Binding FilePath, Mode=OneWay}" Margin="5" ToolTip="{Binding FilePath}"/>
        <TextBox Text="{Binding ShaderCode, Mode=OneWay}" Margin="5" MinHeight="10"
                 ScrollViewer.HorizontalScrollBarVisibility="Visible"
                 ScrollViewer.VerticalScrollBarVisibility="Visible"/>
    </StackPanel>
</UserControl>
