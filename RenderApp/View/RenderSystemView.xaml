<UserControl x:Class="RenderApp.View.RenderSystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <WrapPanel>
            <TextBlock Text="Output"  HorizontalAlignment="Stretch"/>
            <ComboBox ItemsSource="{Binding Model.ProcessingTexture}" SelectedIndex="{Binding TextureIndex}" HorizontalAlignment="Stretch">
                
            </ComboBox>
        </WrapPanel>
        <StackPanel/>
    </StackPanel>
</UserControl>
