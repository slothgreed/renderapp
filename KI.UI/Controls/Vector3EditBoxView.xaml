<UserControl
    x:Class="KI.Presentation.Controls.Vector3EditBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:KI.Presentation.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <WrapPanel>
        <TextBlock
            Width="15"
            Background="Red"
            Text="X"
            TextAlignment="Center" />
        <controls:KITextBox Width="80" Text="{Binding XValue, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
        <TextBlock
            Width="15"
            Background="Green"
            Text="Y"
            TextAlignment="Center" />
        <controls:KITextBox Width="80" Text="{Binding YValue, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
        <TextBlock
            Width="15"
            Background="Blue"
            Text="Z"
            TextAlignment="Center" />
        <controls:KITextBox Width="80" Text="{Binding ZValue, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" />
    </WrapPanel>
</UserControl>
