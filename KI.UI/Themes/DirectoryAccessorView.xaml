<ResourceDictionary
    x:Class="KI.UI.Themes.DirectoryAccessorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="clr-namespace:KI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Style TargetType="{x:Type control:DirectoryAccessor}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type control:DirectoryAccessor}">
                    <DockPanel Margin="2">
                        <TextBlock
                            x:Name="PART_FolderPath"
                            Text="{Binding FolderPath}"
                            ToolTip="{TemplateBinding FolderPath}" />
                        <Button
                            x:Name="PART_RefButton"
                            Width="48"
                            Content="参照"
                            DockPanel.Dock="Right" />
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
